pipeline {
    agent any

    environment {
        GIT_REPO_NAME = "cicd"
        GIT_USER_NAME = "Niviesh"
    }

    stages {
        stage('Build') {
            steps {
                echo 'Build Successful'
            }
        }
    }
}
